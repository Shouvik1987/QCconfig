server:
 port: 8070
   
eureka:
 instance:   
  hostName: localhost
 client:
  fetchRegistry: false
  registerWithEureka: false
  serviceUrl:
   defaultZone: http://${eurkea.instance.hostName}/${server.port}/eureka 
